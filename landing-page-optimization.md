# QtEasyTier 落地页卡片布局与动效优化方案

## 目标

1. 提升首页卡片信息层级，让用户 5 秒内看懂「能做什么、为什么选它」。
2. 优化卡片网格在桌面和移动端的一致性，避免孤卡与高度跳动。
3. 增加更有节奏的动效，同时保证性能与可访问性（含 `prefers-reduced-motion`）。

## 当前页面现状（基于代码）

- 页面入口：`src/content/docs/index.mdx`
- 卡片组件：`src/components/FeatureCards.astro`
- 全局样式：`src/styles/global.css`
- 现有卡片动效：入场上移 + 图标旋转 + hover 抬升（CSS）
- 现有布局：
  - 第一组 `columns=2`（2 张卡）
  - 第二组 `columns=3`（5 张卡）

## 主要问题

1. **网格节奏不稳定**：第二组 5 张卡在大屏 3 列下会出现最后一行孤卡，视觉重心偏移。
2. **卡片层级较平**：所有卡片视觉权重接近，缺少「主卖点卡」。
3. **动效触发时机单一**：当前动画在页面加载时触发，滚动到下方时已错过最佳感知。
4. **可维护性可再收敛**：`FeatureCard.tsx` 与 `FeatureCardGrid.tsx` 目前未被首页使用，可考虑清理。

## 布局优化方案

### 方案 A（推荐，改动小）

1. 第二组 5 卡改为 **`3 + 2` 双行语义布局**：
   - 第一行：性能、易用、一键联机（核心卖点）
   - 第二行：功能丰富、开源免费（信任补充）
2. 第一组 2 卡保留，但把「远程组网」设为主卡（更大内边距 + 渐变边框）。
3. 卡片统一最小高度（例如 `min-height: 220px`），避免描述文字长短造成视觉抖动。

### 方案 B（中等改动）

1. 使用 `auto-fit + minmax(260px, 1fr)` 自适应网格替代固定列数。
2. 对主卡使用 `grid-column: span 2`（仅桌面端），形成明确视觉锚点。
3. 在移动端降级为单列，保持阅读顺序不变。

## 动效优化方案

### 1) 入场动效（滚动触发）

- 用 `IntersectionObserver` 给卡片添加 `is-visible`，只在进入视口时播放。
- 节奏建议：`40ms ~ 80ms` 递增 stagger，单卡时长 `420ms ~ 520ms`。
- 动效形态：`opacity + translateY(16px)`，避免大幅缩放导致眩晕感。

### 2) Hover 微交互（桌面端）

- 卡片：`translateY(-4px)` + 阴影加深。
- 图标：`scale(1.06)`，取消大角度旋转，减少“花哨感”。
- 增加轻微高光扫过（伪元素）强化质感，持续时间 `220ms`。

### 3) 移动端反馈

- 用 `:active` 或 `tap` 态替代 hover，反馈时长控制在 `120ms ~ 180ms`。

### 4) 可访问性与性能

1. 保留并强化 `@media (prefers-reduced-motion: reduce)`，关闭非必要动画。
2. 动画属性只使用 `transform/opacity`，避免触发布局重排。
3. 避免引入更重运行时；首页当前场景建议优先 CSS + 原生 API。

## 推荐落地步骤（按优先级）

### P0（当天可完成）

1. 重排第二组卡片为 `3 + 2`，补齐信息层级。
2. 加入主卡样式（强调「远程组网」）。
3. 将入场动画改为滚动触发，保留 reduced-motion 分支。

### P1（1~2 天）

1. 增加卡片高光扫过与按钮统一动效语言。
2. 优化「相关链接」「联系方式」区块的卡片化视觉一致性。
3. 清理未使用组件：`src/components/FeatureCard.tsx`、`src/components/FeatureCardGrid.tsx`（确认无其它引用后）。

### P2（可选）

1. 做一次 Lighthouse 复测（重点看 CLS、INP）。
2. 根据真实访问数据微调动画时长与延迟。

## 验收标准

1. 桌面端（>=1024px）第二组不出现孤卡视觉失衡。
2. 375px/768px/1024px 下卡片内容不溢出、无横向滚动。
3. 动效在弱性能设备上无明显掉帧；`reduced-motion` 生效。
4. 首屏 CTA 仍保持最高视觉优先级，不被卡片区抢焦点。

## 额外建议

- 你现在首页已经有不错的基础视觉风格（玻璃拟态 + 青蓝主色），本轮建议重点做「结构层级 + 动效时机」而不是继续叠加特效。
- 如果你愿意，我可以下一步直接按 P0 帮你把首页代码改好（含响应式与动效实现）。
